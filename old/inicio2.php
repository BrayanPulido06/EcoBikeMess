<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EcoBikeMess - Mensajería Ecológica</title>
    <link rel="stylesheet" href="inicio2.css">

</head>
<body>
    <!-- Navegación -->
    <nav class="navbar" id="navbar">
        <div class="nav-container">
            <a href="#" class="logo">
                <img src="../public/img/logonegro.png" alt="EcoBikeMess">
                EcoBikeMess
            </a>
            <ul class="nav-menu">
                <li><a href="#inicio">Inicio</a></li>
                <li><a href="#servicios">Servicios</a></li>
                <li><a href="#nosotros">Nosotros</a></li>
                <li><a href="#contacto">Contacto</a></li>
            </ul>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero-section" id="inicio">
        <div class="hero-leaves"></div>
        <div class="hero-pattern"></div>
        <div class="hero-content">
            <div class="hero-badge">🍃 100% Natural • 100% Eficiente</div>
            <h1 class="hero-title">
                Mensajería <span class="cursive">verde</span> que cuida tu ciudad
            </h1>
            <p class="hero-subtitle">
                "Cada pedalada cuenta una historia de compromiso ambiental. 
                Entregamos más que paquetes: entregamos esperanza para el futuro."
            </p>
            <div class="cta-group">
                <a href="#servicios" class="cta-button cta-primary">
                    Descubrir Servicios
                </a>
                <a href="#contacto" class="cta-button cta-secondary">
                    Hablar con Nosotros
                </a>
            </div>
        </div>
    </section>



    <!-- Sección Servicios -->
    <section class="section fade-in" id="servicios">
        <div class="section-header">
            <div class="section-badge">Servicios</div>
            <h2 class="section-title">Soluciones que respiran verde</h2>
            <p class="section-subtitle">
                "Nuestros servicios nacen del amor por la naturaleza y el compromiso 
                con la excelencia en cada entrega."
            </p>
        </div>
        <div class="cards-container">
            <div class="card">
                <div class="card-icon">🌪️</div>
                <h3>Express Verde</h3>
                <p>Como el viento entre las hojas, nuestro servicio express llega en menos de 2 horas. Documentos urgentes, medicinas y productos esenciales entregados con la velocidad de la naturaleza y el cuidado que mereces.</p>
            </div>
            <div class="card">
                <div class="card-icon">📦</div>
                <h3>Paquetería Orgánica</h3>
                <p>Entrega programada que fluye como los ciclos naturales. Cada paquete es tratado con el respeto que damos a la tierra, optimizando rutas como los ríos encuentran su camino al mar.</p>
            </div>
            <div class="card">
                <div class="card-icon">🌍</div>
                <h3>Logística Sustentable</h3>
                <p>Soluciones integrales que abrazan la filosofía de cero desperdicios. Creamos ecosistemas logísticos donde cada movimiento nutre el futuro de nuestra ciudad y nuestro planeta.</p>
            </div>
        </div>
    </section>

    <!-- Sección Nosotros -->
    <section class="section fade-in" id="nosotros">
        <div class="section-header">
            <div class="section-badge">Nuestra Esencia</div>
            <h2 class="section-title">Raíces profundas, impacto duradero</h2>
            <p class="section-subtitle">
                "Como un árbol que crece fuerte desde sus raíces, 
                nuestros valores nos sostienen en cada decisión."
            </p>
        </div>
        <div class="cards-container">
            <div class="card">
                <div class="card-icon">🌟</div>
                <h3>Nuestra Visión</h3>
                <p>Imaginamos ciudades que respiran, donde cada entrega es una semilla de cambio. Ser el bosque que oxigena la logística urbana, creando un legado verde para las generaciones que vienen.</p>
            </div>
            <div class="card">
                <div class="card-icon">🚴‍♀️</div>
                <h3>Nuestra Misión</h3>
                <p>Pedalear hacia un futuro mejor, entrega por entrega. Conectamos corazones y hogares a través de un servicio que late al ritmo de la naturaleza, responsable y lleno de vida.</p>
            </div>
            <div class="card">
                <div class="card-icon">💚</div>
                <h3>Nuestros Valores</h3>
                <p>Amor por la tierra, respeto por las personas, pasión por la excelencia. Cada valor es como una hoja en nuestro árbol: individual pero parte de algo más grande y hermoso.</p>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer id="contacto">
        <div class="footer-wave"></div>
        <div class="footer-content">
            <div class="footer-brand">
                <h3>EcoBikeMess</h3>
                <p>Somos más que mensajería; somos un movimiento. Cada pedalada es un acto de amor hacia nuestra Tierra. Únete a nosotros en este viaje hacia un futuro más verde y esperanzador.</p>
                <div class="cta-group">
                    <a href="https://wa.link/49g8jg" class="cta-button cta-primary">Comenzar Ahora</a>
                </div>
            </div>
            <div class="footer-links">
                <h4>Contáctanos</h4>
                <ul>
                    <li><a href="https://wa.link/49g8jg">🌱 +57 312 318 06 19</a></li>
                    <li><a href="mailto:Eco.BikeMess@gmail.com">🍃 Eco.BikeMess@gmail.com</a></li>
                    <li><a href="https://www.google.com/maps/place/Eco+BikeMess/@4.6481855,-74.0684432,19z">🌿 Calle 61 #17-15, Bogotá</a></li>
                </ul>
            </div>
            <div class="footer-links">
                <h4>Síguenos</h4>
                <ul>
                    <li><a href="#">🌺 Facebook</a></li>
                    <li><a href="#">🌸 Instagram</a></li>
                    <li><a href="#">🌻 LinkedIn</a></li>
                    <li><a href="#">🌷 Twitter</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2025 EcoBikeMess. Cultivando un futuro mejor, entrega por entrega. 🌱</p>
        </div>
    </footer>

    <!-- Botón WhatsApp -->
    <a href="https://wa.link/49g8jg" class="whatsapp-float" target="_blank" title="Conversemos por WhatsApp">💬</a>

    <script>
        // Crear hojas que caen
        function createFallingLeaves() {
            const leavesContainer = document.querySelector('.hero-leaves');
            const leafCount = 15;

            for (let i = 0; i < leafCount; i++) {
                const leaf = document.createElement('div');
                leaf.classList.add('leaf');
                
                leaf.style.left = Math.random() * 100 + '%';
                leaf.style.animationDelay = Math.random() * 8 + 's';
                leaf.style.animationDuration = (Math.random() * 6 + 6) + 's';
                
                leavesContainer.appendChild(leaf);
            }
        }

        // Navbar scroll effect
        window.addEventListener('scroll', () => {
            const navbar = document.getElementById('navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Smooth scrolling
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    const offsetTop = target.offsetTop - 80;
                    window.scrollTo({
                        top: offsetTop,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Fade in animation
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in').forEach(el => {
            observer.observe(el);
        });

        // Counter animation con sufijos personalizados
        function animateCounters() {
            const counters = document.querySelectorAll('.stat-number');
            
            counters.forEach(counter => {
                const target = parseInt(counter.getAttribute('data-target'));
                let current = 0;
                const increment = target / 60;
                
                const timer = setInterval(() => {
                    current += increment;
                    if (current >= target) {
                        let suffix = '+';
                        if (target === 2500) suffix = '+';
                        else if (target === 99) suffix = '%';
                        else if (target === 50) suffix = '';
                        else suffix = '+';
                        
                        counter.textContent = target + suffix;
                        clearInterval(timer);
                    } else {
                        let suffix = '+';
                        if (target === 2500) suffix = '+';
                        else if (target === 99) suffix = '%';
                        else if (target === 50) suffix = '';
                        else suffix = '+';
                        
                        counter.textContent = Math.floor(current) + suffix;
                    }
                }, 50);
            });
        }

        // Trigger counter animation when stats section is visible
        const statsSection = document.querySelector('.stats-section');
        const statsObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    animateCounters();
                    statsObserver.unobserve(entry.target);
                }
            });
        }, { threshold: 0.3 });

        if (statsSection) {
            statsObserver.observe(statsSection);
        }

        // Parallax effect suave
        window.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            const heroContent = document.querySelector('.hero-content');
            const heroPattern = document.querySelector('.hero-pattern');
            
            if (heroContent) {
                heroContent.style.transform = `translateY(${scrolled * 0.3}px)`;
            }
            if (heroPattern) {
                heroPattern.style.transform = `translateY(${scrolled * 0.1}px)`;
            }
        });

        // Efecto hover orgánico para cards
        document.querySelectorAll('.card').forEach(card => {
            card.addEventListener('mouseenter', () => {
                card.style.transform = 'translateY(-10px) rotate(1deg)';
            });
            
            card.addEventListener('mouseleave', () => {
                card.style.transform = 'translateY(0) rotate(0deg)';
            });
        });

        // Inicializar efectos cuando carga la página
        document.addEventListener('DOMContentLoaded', function() {
            createFallingLeaves();
        });

        // Mobile menu functionality (si es necesario)
        const toggleMobileMenu = () => {
            const navMenu = document.querySelector('.nav-menu');
            navMenu.classList.toggle('active');
        };
    </script>
</body>

</html>